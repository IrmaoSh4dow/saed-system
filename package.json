{
  "name": "saed-system",
  "version": "0.1.0",
  "private": true,
  "description": "SAED Management System monorepo (api + web)",
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "dev:api": "npm run start:dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "build:api": "npm run build --prefix api",
    "build:web": "npm run build --prefix web"
  }
}
